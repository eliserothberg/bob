var auth = process.env.auth_key;

console.log("key.js auth key = " + auth);
module.exports = auth;

//only operate on the JSON but download it with keys
// one test file per source file do a file per test